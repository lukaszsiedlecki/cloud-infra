apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaNodePool
metadata:
  name: dual-role
  namespace: kafka
  labels:
    strimzi.io/cluster: my-cluster
spec:
  # Single broker — a pet project with no real traffic doesn't justify
  # doubling the compute/storage footprint for quorum resilience, consistent
  # with every other cost-over-resilience call made in this environment
  # (no backups, no HA, spot everywhere). No podAntiAffinity needed with
  # only one replica.
  replicas: 1
  roles:
    - controller
    - broker
  storage:
    type: jbod
    volumes:
      - id: 0
        type: persistent-claim
        size: 10Gi
        storageClass: standard-rwo
        deleteClaim: false
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 1Gi
  template:
    pod:
      # Spot Pods, same as the app Deployments — accepted tradeoff, see
      # k8s/kafka/README.md.
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: cloud.google.com/gke-spot
                    operator: In
                    values:
                      - "true"
      tolerations:
        - key: cloud.google.com/gke-spot
          operator: Equal
          value: "true"
          effect: NoSchedule
---
apiVersion: kafka.strimzi.io/v1beta2
kind: Kafka
metadata:
  name: my-cluster
  namespace: kafka
  annotations:
    strimzi.io/node-pools: enabled
    strimzi.io/kraft: enabled
spec:
  kafka:
    version: 4.2.0
    # No external listener — nothing outside the cluster needs to reach
    # Kafka, and a LoadBalancer Service would add another standing
    # forwarding-rule cost. See k8s/kafka/README.md.
    listeners:
      - name: plain
        port: 9092
        type: internal
        tls: false
    config:
      # Replication factor 1 — only one broker exists, so anything higher
      # would prevent topic creation entirely.
      offsets.topic.replication.factor: 1
      transaction.state.log.replication.factor: 1
      transaction.state.log.min.isr: 1
      default.replication.factor: 1
      min.insync.replicas: 1
  entityOperator:
    topicOperator: {}
    userOperator: {}
